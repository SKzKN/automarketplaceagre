services:
  - type: web
    name: estonian-car-api
    runtime: docker
    region: frankfurt
    plan: free
    dockerfilePath: ./Dockerfile.render
    dockerContext: .
    healthCheckPath: /health
    envVars:
      - key: PORT
        value: 8000
